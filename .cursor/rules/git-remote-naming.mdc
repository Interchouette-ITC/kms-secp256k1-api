---
description: Reminder - Interchouette remotes (canonical rule is ~/.cursor/rules)
alwaysApply: true
---

# Interchouette GitHub (kms-secp256k1-api reminder)

Canonical always-on rule: **`~/.cursor/rules/git-remote-naming.mdc`** (applies to all repos). Follow that rule for `gh` auth. Do not document local credential paths in this repo.

Quick locks for this tree:

| Item | Value |
| --- | --- |
| Worker remotes | **`interchouette`** = Interchouette fork; **`upstream`** = Interchouette-ITC; never worker as `origin` |
